# Audit Service

FastAPI service for immutable audit snapshots.

## Local endpoints

- `GET /health`
- `GET /audit/orders/{correlationId}`

## Configuration

- `SERVICE_NAME`
- `SERVICE_PORT`
- `DATABASE_URL`
- `RABBITMQ_URL`
